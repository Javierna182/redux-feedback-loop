function Supported() {
    return(
        <div>
            <h1>How are you being Supported?</h1>
            <input
              placeholder="Supported?"
            ></input>
            <button>NEXT</button>
        </div>
    );
}

export default Supported;